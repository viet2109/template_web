import Header from "../components/Header";

interface Props {}

function DefaultLayout(props: Props) {
  const {} = props;

  return (
    <main>
      <Header />
    </main>
  );
}

export default DefaultLayout;
